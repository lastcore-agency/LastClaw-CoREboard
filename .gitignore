node_modules/
dist/
.env
.env.*
!.env.example
.DS_Store
__MACOSX/
.vite/
*.log
*.tsbuildinfo
dist-server
artifacts/screenshots/live-runtime/
test-results/
# Compiled JS artifacts that may appear in src/ from tsc or VM runs
src/**/*.js
src/**/*.d.ts
!src/vite-env.d.ts

# ── Local-only artifacts (never commit) ───────────────────────
# Screenshots and verification captures
artifacts/screenshots/
capture v1/

# ZIP archives — staging assets, not source
Archive *.zip
*.zip
!package-lock.json

# Agent tooling config (local per-machine)
.agents/
lastclaw/.agentsroom/
lastclaw/.mcp.json

# Stray tag/ref files
ffice-origin-v0.1.0^{}

# Asset staging (raw working copies, not production)
asset/
